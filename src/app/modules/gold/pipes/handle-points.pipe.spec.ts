import { HandlePointsPipe } from './handle-points.pipe';

describe('HandlePointsPipe', () => {
  it('create an instance', () => {
    const pipe = new HandlePointsPipe();
    expect(pipe).toBeTruthy();
  });
});
