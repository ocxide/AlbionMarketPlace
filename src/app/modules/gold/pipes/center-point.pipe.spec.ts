import { CenterPointPipe } from './center-point.pipe';

describe('CenterPointPipe', () => {
  it('create an instance', () => {
    const pipe = new CenterPointPipe();
    expect(pipe).toBeTruthy();
  });
});
